existem x = 10;
velocidade recebe x + 5;

se x > 5 ->ent {
    velocidade recebe 0;
}

paraTodo i de 0 ate 10 passo 1 {
    velocidade recebe velocidade + 1;
}

dadoQue (velocidade > 0 E inclinacao < 30) {
    x recebe x + 1;
}